package UseCases;
import bdd.MongoController;
import model.*;

public class TicketCases {
    MongoController controller = new MongoController(null);
    public static void generateTicket(User u, Event e){

    }
}
